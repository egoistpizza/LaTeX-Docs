\documentclass[12pt]{article}

\begin{document}

% LaTeX yazısının etrafındaki ters slash işaretleri onu alışılmış yazılışında gösterir.
Hello! This is my first \LaTeX\ doc! 

% Satırlar arasında geçiş sağlamak için // operatörü kullanılır. Yeni paragraf istiyorsak basitçe araya boş satır bırakarak yeni satıra atlarız.

% $ işaretleri arasındaki karakterler MathMode varsayılır. 
A rectangle has side lengths of $(x+1)$ and $(x+3)$.

% {} operatörlerinin arasındaki ifadenin bütünlüğü korunur.
% $$ operatörü arasındaki ifadeyi yeni satıra aktararak daha iyi bir görselleştirme sunar.
The equation $${A(x) = x^2 + 4x + 3}$$ gives the area of the rectangle.

\end{document}